# ESP-IDF build output + generated config
build/
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock

# Real venue Wi-Fi credentials — copy wifi_creds.example.h → wifi_creds.h.
# NEVER commit the real one.
main/wifi_creds.h
