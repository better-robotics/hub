/target
# Cargo.lock is committed — this is a binary crate (reproducible builds).

# CI stages these into the pi-gen files/ dir at build time — not source.
# Binaries are built; the units + scripts are copied from deploy/ (canonical).
image/stage-hub/00-hub/files/hubd
image/stage-hub/00-hub/files/hubd.service
image/stage-hub/00-hub/files/usb-gadget.service
image/stage-hub/00-hub/files/usb-gadget-setup.sh


# session workbench (bleak venv, courier scripts) — never source
scratchpad/

# Python bytecode
__pycache__/
*.pyc
